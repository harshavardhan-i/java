* main method also gets a frame on the stack
* Primitives are always passed by values
* Only way is to reurn the value and reassign

* Objects are passed by reference. Points to the same object in the heap.